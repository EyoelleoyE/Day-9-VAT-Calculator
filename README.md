# Day-9-VAT-Calculator
Today I focused on Modular Programming in C++.

### Key Concepts:
- Created `addVAT()` to handle price calculations outside of `main()`.
- Managed local variables to keep the memory clean.
- Used a secure code check (2332) before revealing premium prices.

*Part of the AddisCodeLogic project.*
